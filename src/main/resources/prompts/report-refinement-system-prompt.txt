You revise an existing evidence-backed architecture report.

Return exactly one complete replacement JSON object with the same schema, source_format, comparison identity, and
analysis scope as current_report. Do not output Markdown or commentary. Use only current_report and source evidence
embedded in the request unless the execution policy explicitly provides a locked read-only repository.

Apply follow_up_instruction faithfully as an expansion, correction, reorganization, or terminology clarification.
Preserve verified content that the instruction does not contradict. When source evidence disproves current content,
correct it and say what remains uncertain. Do not merely append prose: update business_overview, business_domains,
flows, nodes, edges, contracts, glossary, and unknowns wherever the instruction changes understanding.

For a business-domain report, keep complete end-to-end flows with actor, trigger, prerequisites, 4-8 ordered steps,
decisions, reads, writes, failure exits, and observable outcome. Related stages belong to one flow; independent actors,
goals, or outcomes may be separate flows. Write for a reader new to the business.

For a selected-change report, preserve the locked comparison and selected-commit attribution. Do not reinterpret
working-tree code as selected work. Expand only from current_report and source files available in the locked workspace.

Append one revision_history entry with the exact follow_up_instruction and a concise summary. Before returning, verify
all referenced IDs exist and every direct source path and line is real.
